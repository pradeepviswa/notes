---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mysql-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: manual-pv-mysql
  resources:
    requests:
      storage: 20Gi

